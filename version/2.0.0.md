# 2026.06.02

## 更新内容：
- 重新书写了 *README.md* 并更改了部分文件命名
- 整理了文件结构，明确了版本修改的控制
- 修改了电机的设置
- 将控制串口、电机控制和底盘控制的底层重新书写，**主程序中部分调用方式需要修改**
    - 在语法上修改了全局单例的获取方式，主程序中需要修改
    - 部分函数修改了名字和参数列表，如果不兼容原版本请修改主程序中的调用方式
    - 增加了多条电机指令同时发送的功能，详情见具体的 *README.md*
    - 取消了每次发送完消息后的固定时间阻塞，改为等待至发送完就返回（具体时长取决于信号长度），但调用时仍需要手动增加间隔（相邻两次发送操作不可连续进行，否则可能会“吞信号”）
    - 增加了多线程保护和线程锁，但注意仍然不可以同时发送，只能保证信号不会串，后来的线程会等待直到正在使用串口的线程调用完后才能继续
    - 暂时未增加电机运动的新功能